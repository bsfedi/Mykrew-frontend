export const environment = {
  production: false,
  baseUrl: 'https://mykrew-backend.onrender.com/',// Change this to your local development server URL
  default: ''
};

console.log("dev", environment.baseUrl);
